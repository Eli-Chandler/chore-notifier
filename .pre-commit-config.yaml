repos:
  - repo: local
    hooks:
      - id: dotnet-format
        name: dotnet format
        entry: dotnet format ChoreNotifier.sln --verify-no-changes
        language: system
        pass_filenames: false
        types: [c#]
        description: Run dotnet format on the solution (includes all projects and tests)